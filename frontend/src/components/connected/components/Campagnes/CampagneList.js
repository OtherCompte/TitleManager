import React from 'react'

export default function CampagneList() {

    // const { users, connectedUser } = useSelector(state => ({
    //     ...state.UserReducer
    // }))

    // const user = users.filter(user => user.id === connectedUser)[0]
        
    return (
        <div>
            {/* Display campaign Form to seeCampaignList*/}
        </div>
    )
}
